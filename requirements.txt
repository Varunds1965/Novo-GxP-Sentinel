# M0-M2 Core (already satisfied by standard library)
# - No dependencies required for deterministic core, ingestion, or security

# M3: API & Backend (minimal dependencies for web service)
Flask==3.0.0
Werkzeug==3.0.0
click==8.1.7
itsdangerous==2.1.2
Jinja2==3.1.2
MarkupSafe==2.1.3

# M3: Database & Persistence
# SQLite is built-in to Python 3.12

# M5: Local AI (to be added in M5)
# llama-cpp-python==0.2.X (when adding local model)

# Development & Testing (optional)
pytest==7.4.0
coverage==7.2.0
black==23.9.1
flake8==6.0.0
